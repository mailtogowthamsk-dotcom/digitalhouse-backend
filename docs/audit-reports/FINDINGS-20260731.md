# Live audit findings — 2026-07-31 (PR-D)

**Policy:** [DATABASE_POLICY.md](../DATABASE_POLICY.md) · **Checklist:** [DATABASE_LIVE_AUDIT.md](../DATABASE_LIVE_AUDIT.md)  
**Raw report:** [live-20260731.md](./live-20260731.md)  
**Constraint:** Read-only. **No DDL** was run. Remediation requires separate approved tickets / versioned migrations.

---

## Meta

| Field | Value |
|-------|--------|
| Environment | Live (`infosense_digital_house` via configured `DB_*`) |
| Staging | Not separately available in this workspace — same credentials used for prior smoke |
| Date | 2026-07-31 |
| MySQL | 8.0.43 |
| Runner | `npm run db:audit:live` |
| `db:migrate:status` | 1 total, **0 applied**, **1 pending** (`20260731130000_media_jobs_queue`) |

---

## Summary table (§G)

| ID | Finding | Severity | Suggested follow-up (additive only; separate ticket) |
|----|---------|----------|------------------------------------------------------|
| F1 | `schema_migrations` table **missing**; versioned migrate history never started on this host | **High** | On next approved deploy: `npm run db:migrate` (creates history + applies pending). Confirm backup first per policy §7. |
| F2 | `media_jobs` table **absent** (only `media_files` exists). Media worker queue cannot run on this DB | **High** | Same as F1 — pending migration `media_jobs_queue` is the intended path. Do **not** use legacy `db:run-media-jobs-sql` unless incident + `ALLOW_LEGACY_DB_SCRIPTS=1`. |
| F3 | Junction tables already have UNIQUE pair indexes; **0** duplicate groups | Clean | Roadmap P2 (add UNIQUE after dedupe) **not needed** for `post_likes` / `saved_posts` / `post_hashtags` / matrimony pairs on this host. |
| F4 | Matrimony FKs present (**24**); core social FKs present for posts/likes/saves/messages/notifications | Clean | Roadmap P3 (add missing matrimony FKs) **not blocked** by missing pair FKs here. Orphan counts not measured (out of runner scope). |
| F5 | `users.status` ENUM includes soft-delete / review set (`SUSPENDED`, `CHANGES_REQUESTED`, `DELETED`, …) | Clean | Do **not** re-run legacy ENUM `MODIFY` scripts. |
| F6 | Feed EXPLAIN: `idx_posts_type_created`, no filesort/temp | Clean | — |
| F7 | Notifications EXPLAIN: `idx_notif_user_deleted_created`, covering, no filesort | Clean | — |
| F8 | Messages EXPLAIN: uses `idx_messages_pair_created` but **Using filesort** (bidirectional OR) | Low | Optional later: evaluate query shape / covering index; not freeze-breaking. Measure with real pair ids before changing indexes. |
| F9 | Possible redundant non-unique indexes alongside UNIQUEs (e.g. `users.email` + `users_email`) | Low | Inventory-only; drop only with separate ticket if proven unused. |

---

## Checklist fill-in

### A. schema_migrations

| Check | Result | Notes |
|-------|--------|-------|
| Table exists | **No** | F1 |
| Applied versions | n/a | `db:migrate:status` → 0 applied |
| Unexpected empty on long-lived env? | **Yes** | Host has full app schema from legacy path; versioned framework not bootstrapped |

### B. ENUM — focus

| Column | Match? |
|--------|--------|
| `users.status` | **Yes** — expected soft-delete/review values present |
| `posts.postType` | Present (ANNOUNCEMENT…ENTERTAINMENT) — compare to `POST_TYPES` in app if constants change |
| media / moderation | `media_files.processingStatus`, `posts.moderation_status` present |

### C. Indexes (notable)

| Table | Present? | Notes |
|-------|----------|-------|
| posts | Yes | `idx_posts_type_created`, moderation, job/mp/help helpers |
| media_jobs | **No table** | F2 |
| messages | Yes | `idx_messages_pair_created` |
| notifications | Yes | user/deleted/created covering |
| junctions | Yes | UNIQUE on likes/saves/hashtags + matrimony pairs |

### D. Foreign keys

| Area | Result |
|------|--------|
| Core social | Present |
| Matrimony suite | 24 FKs |
| media_jobs → media_files | N/A (no `media_jobs`) |

### E. Duplicates

| Table | Duplicate groups |
|-------|------------------|
| post_likes / saved_posts / post_hashtags | **0** |
| matrimony_interests / matrimony_matches | **0** |

### F. EXPLAIN

| Query | key | filesort | temporary | Acceptable? |
|-------|-----|----------|-----------|-------------|
| Feed | `idx_posts_type_created` | no | no | Yes |
| Messages | `idx_messages_pair_created` | **yes** | no | Acceptable for now (F8) |
| Notifications | `idx_notif_user_deleted_created` | no | no | Yes |

---

## Recommended next tickets (not part of PR-D)

1. **Deploy migrate bootstrap** — backup verify → `npm run db:migrate` on this host (addresses F1 + F2). Abort on failure.
2. **Confirm media worker** after migrate — claim/process smoke; ops docs already cover log rotation.
3. **Optional** — messages filesort investigation (F8); orphan-row counts before any new FKs.
4. **Defer** — ENUM→STRING, CHECK, renames, polymorphic posts split (freeze).

---

## Sign-off

| Item | Status |
|------|--------|
| Staging-first | Skipped (no separate staging `DB_*` in workspace); live audit executed read-only |
| Prod/live audit filed | Done — this document + raw report |
| DDL applied | **None** |
| Schema freeze respected | Yes |
