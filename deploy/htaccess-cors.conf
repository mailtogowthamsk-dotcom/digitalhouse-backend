# DEPRECATED when using Node + PM2 proxy (htaccess-combined.conf).
#
# Apache CORS + OPTIONS 204 here often causes:
# - Duplicate Access-Control-Allow-Origin (Apache + Node)
# - OPTIONS answered without proper CORS headers
#
# Use htaccess-combined.conf (proxy only). Node handles CORS.
